# Julia

[Julia](https://julialang.org/) is a dynamically typed JIT language often used
in scientific computing.

## Installation

```sh
sudo apt install julia
```

## Running

```sh
julia cat.jl < README.md
```
